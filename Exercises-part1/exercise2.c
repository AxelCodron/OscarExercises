// Passons maintenant aux fonctions !
// Une fonction est un morceau de code qui peut être appelé plusieurs fois dans ton programme.
// Par exemple, si tu as une fonction qui calcule la somme de deux nombres,
// tu peux l'appeler plusieurs fois pour calculer la somme de 2+3, 2+4, 2+5, etc.
// Une fonction DOIT donner le type qu'elle renvoie, et PEUT prendre des arguments.
// Par exemple, la fonction "int somme(int a, int b)" prend deux entiers en arguments, renvoie un entier, et porte le nom "somme".
// Pour utiliser cette fonction, on peut écrire "somme(2, 3)".

// Consigne : complète la fonction "FoncionQuiAjouteDeuxNombre" pour qu'elle renvoie la somme de ses deux arguments.
// Indice : a et b sont des variables, ce qui veut dire qu'elles prennent la valeur de ce qu'on leur donne.
// Par exemple, si tu écris addition(2, 3), alors a prend la valeur 2, et b prend la valeur 3.

int FoncionQuiAjouteDeuxNombre(int a, int b)
{
    // ton code ici !
}
